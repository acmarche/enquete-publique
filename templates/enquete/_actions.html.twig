<a href="{{ path('enquete_edit', { 'id': enquete.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-pencil"></i> Editer</a>

<a href="{{ path('enquete_localisation_edit', { 'id': enquete.id }) }}" role="button"
   class="btn btn-primary"> <i class="ti ti-map"></i> Localisation</a>

<a href="{{ path('enquete_document_new', { 'id': enquete.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-folder-plus"></i> Ajouter un document</a>

<a href="{{ path('enquete_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer
</button>

<br/><br/>
