<ul class="nav nav-pills">
    <li class="nav-item"><a href="{{ path('enquete_index') }}" class="nav-link" aria-current="page">Enquete publique</a>
    </li>
    {% if is_granted('ROLE_ENQUETE_ADMIN') %}
        <li class="nav-item"><a href="{{ path('enquete_new') }}" class="nav-link">Ajouter</a></li>
    <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link">{{ app.user }}</a></li>
    {% endif %}
</ul>
